spring:
  application:
    name: reviews-service
  cloud:
    config:
      uri: http://localhost:7777
      fail-fast: true

server:
  port: 9090
---
spring:
  profiles: docker

  cloud:
    config:
      uri: http://config-server:8080
      fail-fast: true