spring:
  application:
    name: reviews-service
  cloud:
    config:
      uri: http://localhost:7777
      fail-fast: true

---
spring:
  profiles:
    active: docker

  cloud:
    config:
      uri: http://config-server:7777
      fail-fast: true