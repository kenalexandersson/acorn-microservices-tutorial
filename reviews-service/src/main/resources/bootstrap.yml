spring:
  application:
    name: reviews-service
  cloud:
    config:
      uri: http://localhost:7777
      fail-fast: true

---
spring:
  profiles: docker

  cloud:
    config:
      uri: http://config-server:7777
      fail-fast: true