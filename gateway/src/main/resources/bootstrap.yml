spring:
  application:
    name: gateway
  cloud:
    config:
      fail-fast: true
      uri: http://localhost:7777

server:
  port: 20202

---
spring:
  profiles:
    active: docker

  cloud:
    config:
      uri: http://config-server:7777
      fail-fast: true