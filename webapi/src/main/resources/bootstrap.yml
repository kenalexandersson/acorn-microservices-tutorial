spring:
  application:
    name: webapi
  cloud:
    config:
      uri: http://localhost:7777
      fail-fast: true

feign:
  hystrix:
    enabled: true

---
spring:
  profiles:
    active: docker

  cloud:
    config:
      uri: http://config-server:7777
      fail-fast: true