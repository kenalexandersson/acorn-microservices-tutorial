spring:
  application:
    name: webapi
  cloud:
    config:
      uri: http://localhost:7777
      fail-fast: true

feign:
  hystrix:
    enabled: true

server:
  port: 8100
---
spring:
  profiles: docker

  cloud:
    config:
      uri: http://config-server:8080
      fail-fast: true