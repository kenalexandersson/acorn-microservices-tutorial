spring:
  application:
    name: items-service
  cloud:
    config:
      uri: http://localhost:7777
      fail-fast: true

server:
  port: 8080
---
spring:
  profiles: docker

  cloud:
    config:
      uri: http://config-server:8080
      fail-fast: true